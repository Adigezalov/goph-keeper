# ==============================================
# Goph-Keeper Server Configuration
# ==============================================

# Database Configuration
# Формат: postgres://username:password@host:port/database?options
DATABASE_URI=postgres://keeper_user:your_secure_password@localhost:5432/goph_keeper?sslmode=disable

# Server Configuration
# Адрес и порт, на котором будет запущен сервер
# Примеры: :8080, localhost:8080, 0.0.0.0:8080
RUN_ADDRESS=:8080

# JWT Configuration
# ВАЖНО: Измените этот ключ на случайную строку минимум 32 символа для production!
# Генератор: openssl rand -base64 32
JWT_SECRET=change-this-to-random-32-char-string-in-production

# SMTP Configuration (для отправки email верификации)
# Настройки для Yandex SMTP
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
# Email аккаунт для отправки писем
SMTP_USERNAME=your-email@yandex.ru
# Пароль приложения (не основной пароль аккаунта!)
# Получить в: https://id.yandex.ru/security/app-passwords
SMTP_PASSWORD=your-app-password
# Email отправителя (обычно совпадает с SMTP_USERNAME)
SMTP_FROM=your-email@yandex.ru

# ==============================================
# Дополнительные опции (через аргументы командной строки)
# ==============================================

# Использование:
# go run cmd/goph-keeper/main.go -a :8080 -d "postgres://..." -jwt-secret "secret"

# Флаги:
# -a              Адрес и порт запуска сервера (по умолчанию :8080)
# -d              URI подключения к базе данных
# -jwt-secret     Секретный ключ для подписи JWT токенов
# -smtp-host      SMTP хост (по умолчанию smtp.yandex.ru)
# -smtp-port      SMTP порт (по умолчанию 465)

# ==============================================
# Примечания
# ==============================================

# 1. Создайте базу данных перед запуском:
#    psql postgres
#    CREATE USER keeper_user WITH PASSWORD 'your_secure_password';
#    CREATE DATABASE goph_keeper OWNER keeper_user;
#    GRANT ALL PRIVILEGES ON DATABASE goph_keeper TO keeper_user;
#    \q

# 2. Примените миграции:
#    go run cmd/migrate/main.go

# 3. Запустите сервер:
#    go run cmd/goph-keeper/main.go

# 4. Production настройки:
#    - Используйте sslmode=require для DATABASE_URI
#    - Смените JWT_SECRET на криптографически стойкий ключ
#    - Настройте HTTPS
#    - Используйте environment variables вместо .env файла
#    - Настройте правильные CORS origins

# ==============================================
# Как использовать этот файл
# ==============================================

# Скопируйте этот файл как .env:
#   cp env.example .env
#
# Затем отредактируйте .env со своими значениями
#
# Загрузите переменные в shell:
#   export $(cat .env | xargs)
#
# Или используйте в коде через библиотеку (например, godotenv)

